%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - current_user.friends.each do |friend|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest('knwang@gmail.com'.downcase)}?s=40")
            = link_to friend.full_name
          %td.extra-padding #{friend.line_items.count}
          %td.extra-padding #{friend.inverse_friends.count}
          %td.extra-padding
            = link_to friend, method: :delete do
              %i.icon-remove
