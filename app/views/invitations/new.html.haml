%section.invite.container
  .row
    .span10.offset1
      = form_for @invitation, class: 'invite' do |f|
        %header
          %h1 Invite a friend to join MyFlix!
        %fieldset
          = f.label :recipient_name, "Friend's Name"
          = f.text_field :recipient_name
          = f.label :recipient_email, "Friend's Email Address"
          = f.email_field :recipient_email
          = f.label :message, "Invitation Message"
          = f.text_area :message, placeholder: "Please join this really cool site!", rows: 6, class: "span4"
        %fieldset.action
          %input(type="submit" value="Send Invitation" class="btn")
