<%@categories.each do |category| %>
		<h1><%=link_to category.title %></br></h1>
		<%category.videos.limit(6).each do |video| %>
			<%=link_to (image_tag video.small_cvr_url), video%>
		<% end %>
<% end %>
